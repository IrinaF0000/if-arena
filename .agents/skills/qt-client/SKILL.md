# Skill: Qt Client

Use for `src/battle_qt_client/**`.

## Checklist

- Keep network code outside UI widgets.
- Use signals/slots and queued connections where needed.
- Do not block UI thread.
- Render server snapshots and events.
- Send intentions only.
- Add visible connection and error state.
- Keep controls responsive and accessible.
