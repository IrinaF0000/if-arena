# Default scenario configuration.

feature battle.basic
feature units.classic
feature io.legacy

# Use rngSeed 123 for deterministic local replay, or rngSeed random for default randomness.
# rngSeed random
# Use feature io.legacy debugSummary=true to emit optional debug lines on stderr.
