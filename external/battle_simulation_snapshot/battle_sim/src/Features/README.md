# Features

Feature modules own domain behavior.

Current feature packs:
- `Battle`: battle components, rules, effects, selectors, systems, policies.
- `UnitsClassic`: built-in unit archetypes.

Dependencies flow one way: feature modules use `Core` infrastructure.
