# IO

Input/output adapters live here.

`LegacyCommands` owns the current text scenario format and legacy `UNIT_*` output compatibility.

IO code may adapt external formats to engine actions/events. Battle mechanics stay in feature modules.
